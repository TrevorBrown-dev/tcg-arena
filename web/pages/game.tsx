import { NextPage } from 'next';
import { Game } from '../generated/graphql';

const Game: NextPage = () => {
    return (
        <div>
            <h1>Game</h1>
        </div>
    );
};

export default Game;

mutation AddCardToDeckTemplate(
    $cardId: Float!
    $deckTemplateId: Float!
    $isFoil: Boolean
) {
    addCardToDeckTemplate(
        cardId: $cardId
        id: $deckTemplateId
        isFoil: $isFoil
    ) {
        ...DeckTemplateParts
    }
}

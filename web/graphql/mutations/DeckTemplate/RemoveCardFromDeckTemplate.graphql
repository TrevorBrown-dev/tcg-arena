mutation RemoveCardFromDeckTemplate(
    $id: Float!
    $cardId: Float!
    $isFoil: Boolean!
) {
    removeCardFromDeckTemplate(id: $id, cardId: $cardId, isFoil: $isFoil) {
        ...DeckTemplateParts
    }
}

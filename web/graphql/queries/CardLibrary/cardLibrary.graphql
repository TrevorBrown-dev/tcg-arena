query CardsInLibrary($cardLibraryId: Float!) {
    cardsInLibrary(id: $cardLibraryId) {
        amount
        isFoil
        card {
            name
            description
            imageUrl
        }
    }
}
